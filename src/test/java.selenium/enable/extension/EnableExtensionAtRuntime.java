package enable.extension;

public class EnableExtensionAtRuntime {
    public static void main(String[] args) {
        /*
        There are few steps to be followed to enable extension in chrome.
        1. Add CRX Extractor/Downloader to Chrome Browser (manually)
        2. Add SelectorsHub plugin to Chrome Browser (manually)
        3. Capture CRX file for SelectorsHub extension
        4. pass CRX file path in automation script in ChromeOptions
        */
    }
}
